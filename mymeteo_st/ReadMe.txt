Change_Arr.h
it is needed to develope member-finding method - to expand Eraser-method (from symbol) to find members (x_max, y_max+-1) and etc

change_arr.h sym_assembler.h is debugged by g++, but don't be launched on main function in assembling

all is ready only exceptions on ChangeArray (for new - std::bad_alloc)
